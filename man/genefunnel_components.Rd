% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genefunnel-components.R
\name{genefunnel_components}
\alias{genefunnel_components}
\title{Calculate GeneFunnel score components}
\usage{
genefunnel_components(mat, gene_sets, BPPARAM = BiocParallel::bpparam())
}
\arguments{
\item{mat}{An unclassed base numeric or integer matrix, or a numeric
dense/sparse \link[Matrix:Matrix]{Matrix::Matrix} object, with features in rows and samples in
columns. It must have at least one row and column. Row names must be
unique, non-missing, and non-empty. Values may be finite and non-negative,
\code{NA}, or \code{NaN}; negative and infinite values are rejected.}

\item{gene_sets}{A non-empty unclassed named list of unclassed character
vectors. Set names must be unique, non-missing, and non-empty. Member
identifiers must be non-missing and non-empty; duplicate members are
deduplicated in first occurrence order.}

\item{BPPARAM}{A \link[BiocParallel:BiocParallelParam]{BiocParallel::BiocParallelParam} backend. The default is
the currently registered \code{\link[BiocParallel:bpparam]{BiocParallel::bpparam()}} backend.}
}
\value{
A fixed-order named list of aligned base matrices:
\itemize{
\item \code{score}, \code{observed_sum}, \code{penalty}, and \code{balance}: double matrices;
\item \code{effective_size}: an integer matrix;
\item \code{observed_fraction}: a double matrix;
\item \code{status}: character matrices \code{semantic}, \code{observed_sum}, \code{penalty},
\code{balance}, and \code{conditioning}; and
\item \code{scaled}: entries \code{observed_sum}, \code{penalty}, and \code{balance}, each containing
double \code{mantissa} and integer \code{exponent} matrices.
}

Every matrix uses the retained set/sample order and names of \code{\link[=genefunnel]{genefunnel()}}.
Semantic status is \code{"scoreable"}, \code{"too_few_observed"}, or \code{"zero_total"}.
Component availability is \code{"ordinary"}, \code{"scaled"}, or \code{"unavailable"}.
}
\description{
Calculate the authoritative GeneFunnel score together with aligned
magnitude, balance, penalty, and observed-support diagnostics. This is an
additive audit API: it does not change \code{\link[=genefunnel]{genefunnel()}} or reconstruct its
score from rounded components.
}
\details{
For each retained gene-set/sample cell, let \code{effective_size} be the number
of matched members remaining after sample-specific \code{NA}/\code{NaN} omission and
let \code{observed_sum} be their sum. \code{observed_fraction} divides effective size
by the set's globally matched size. It is distinct from declared-set
coverage returned by \code{\link[=gene_set_coverage]{gene_set_coverage()}}.

When at least two values remain and their sum is positive, \code{balance} is
Bulla's evenness index for the within-set shares and the score factorizes as
\code{score = observed_sum * balance}. \code{penalty = observed_sum - score} in exact
arithmetic. All-zero cells have score and penalty zero but undefined
balance. Cells with fewer than two observed values have factual support and
sum diagnostics but undefined score, penalty, and balance.

Finite inputs can make a mathematical component overflow or underflow an R
double even when the authoritative score is representable. Such a component
has ordinary value \code{NA}, status \code{"scaled"}, and a canonical sidecar pair
\verb{(mantissa, exponent)} representing \code{mantissa * 2^exponent}, with
\verb{0.5 <= mantissa < 1}. Ordinary and undefined components have \code{NA} sidecar
pairs. Status \code{"unavailable"} marks an undefined or uncertifiable
diagnostic; it never changes the score.

\code{conditioning} is \code{"safe"} only in the protocol's conservative binary64
region where both component reconstructions are numerically checkable.
\code{"ill_conditioned"} directs callers to the scaled representation rather
than rounded subtraction. \code{"not_applicable"} covers zero totals and cells
with fewer than two observed members.
}
\section{Interpretation}{

Balance describes arithmetic evenness within one observed cell. It is not
co-expression, correlation, coordinated regulation, pathway truth, causal
attribution, uncertainty, or preprocessing invariance. It depends on the
effective member support, feature-specific abundance and dynamic range,
measurement noise, and meaningful-zero assumption.
}

\examples{
mat <- rbind(
    A = c(equal = 2, unequal = 6, missing = 1),
    B = c(equal = 2, unequal = 2, missing = 2),
    C = c(equal = NA, unequal = NA, missing = NA)
)
components <- genefunnel_components(
    mat,
    list(pair = c("A", "B"), triple = c("A", "B", "C")),
    BPPARAM = BiocParallel::SerialParam()
)
components$score
components$observed_sum
components$balance
components$effective_size
}
\references{
Bulla L (1994). "An index of evenness and its associated diversity
measure." \emph{Oikos}, 70(1), 167-171. \doi{10.2307/3545713}.
}
\seealso{
\code{\link[=genefunnel]{genefunnel()}}, \code{\link[=gene_set_coverage]{gene_set_coverage()}},
\code{system.file("COMPONENTS_SPEC.md", package = "genefunnel")}
}
